// Configuracion central de integraciones externas.
// Cuando exista backend, cambia solo estas variables de entorno/build.
export const APP_CONFIG = Object.freeze({
  apiBaseUrl: '',
  apiVersion: 'v1',
  requestTimeout: 8000
});
